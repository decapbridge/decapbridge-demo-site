{
  "name": "decap-bridge-demo-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n vite,decap \"vite dev --open --host\" \"decap-server\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "decap-server": "^3.0.4",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
